# ============ WEB ROUTES IMPORT (Added without touching WhatsApp) ============
# These imports register all web routes from modular files
import sys
import os

# Add current directory to path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

# Import ALL web route modules - they will register themselves with @app.route
try:
    import app.routes.workspace
    import app.routes.web_auth
    import app.routes.web_ask
    import app.routes.billing
    import app.routes.me
    import app.routes.plans
    import app.routes.referrals
    import app.routes.history
    import app.routes.web_session
    import app.routes.accounts
    import app.routes.tax
    import app.routes.subscriptions
    
    print("✅ Web routes imported successfully")
    print("   Available web endpoints:")
    print("   • GET  /api/workspace/limits")
    print("   • POST /api/web/auth/request-otp")
    print("   • POST /api/web/auth/verify-otp")
    print("   • POST /api/web/ask")
    print("   • GET  /api/me")
    print("   • GET  /api/billing/me")
    
except ImportError as e:
    print(f"⚠️ Could not import some web routes: {e}")
except Exception as e:
    print(f"⚠️ Error importing web routes: {e}")
# ============ END WEB ROUTES IMPORT ==========
